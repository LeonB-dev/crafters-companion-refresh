{% liquid
  assign template_name = 'section-testimonials'

  assign title = section.settings.title
  assign margin_top = section.settings.margin_top
  assign margin_bottom = section.settings.margin_bottom
  assign padding_top = section.settings.padding_top
  assign padding_bottom = section.settings.padding_bottom
%}

<div
  class="{{- template_name }} overflow-hidden"
  data-section-type="{{- template_name -}}"
  data-section-id="{{ section.id }}"
  id="{{ section.id }}"
>
  <div class="container">
    <div class="content">
      <div class="heading">
        <h2>{{ title }}</h2>
      </div>
    </div>
  </div>

  <simple-slider class="{% if section.blocks.size > 5 %}simple-slider--fullwidth{% endif %}">
    <ul
      id="testimonial-carousel-{{ section.id }}"
      class="simple-slider results-list"
    >
      {% for block in section.blocks %}
        {% liquid
          assign testimonialStarRating = block.settings.star_rating
          assign testimonialTitle = block.settings.title
          assign testimonialContent = block.settings.content
          assign testimonialCitation = block.settings.citation
        %}
        <li class="testimonial">
          <ul class="star-rating">
            {% for i in (1..testimonialStarRating) %}
              <li>{%- render 'icon', _name: 'icon-star' -%}</li>
            {% endfor %}
          </ul>
          <h3>{{ testimonialTitle }}</h3>
          {{ testimonialContent }}
          <p class="citation">{{ testimonialCitation }}</p>
        </li>
      {% endfor %}
    </ul>

    {% render 'simple-slider-ui', _items: section.blocks %}
  </simple-slider>
</div>

<style>
  #{{ section.id }}.section-testimonials {
    margin-top: {{ margin_top }}px;
    margin-bottom: {{ margin_bottom }}px;
    padding-top: {{ padding_top }}px;
    padding-bottom: {{ padding_bottom }}px;
  }
</style>

{% stylesheet %}
.section-testimonials {
  background-color: var(--colour-purple-x-light);
}
.section-testimonials .heading h2 {
  text-align: center;
}
.section-testimonials simple-slider > ul {
  margin-left: 1rem;
}
@media (min-width: 1416px) {
  .section-testimonials simple-slider > ul {
    justify-content: center;
    margin-left: 0;
  }
}
.section-testimonials .testimonial {
  background-color: var(--colour-white);
  border-radius: 1rem;
  padding: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
.section-testimonials .testimonial h3 {
  white-space: normal;
}
.section-testimonials .testimonial p {
  font-size: 1rem;
  margin-bottom: 0;
  text-wrap: wrap;
  white-space: normal;
}
.section-testimonials .testimonial .citation {
  font-weight: 700;
  margin-top: auto;
}
.section-testimonials .testimonial .star-rating {
  list-style: none;
  margin: 0;
  padding: 0;
}
.section-testimonials .testimonial .star-rating li {
  display: inline-flex;
}
.section-testimonials .testimonial .star-rating li svg {
  width: 1rem;
  height: 1rem;
}
{% endstylesheet %}

{% schema %}
{
  "name": "Testimonials",
  "tag": "section",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "id": "title",
      "type": "text",
      "label": "Title",
      "default": "Happy Shoppers, Happy Crafters"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "margin_top",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Margin top",
      "default": 16
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Margin bottom",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding top",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px",
      "label": "Padding bottom",
      "default": 16
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "id": "title",
          "type": "text",
          "label": "Title"
        },
        {
          "id": "content",
          "type": "richtext",
          "label": "Content"
        },
        {
          "id": "citation",
          "type": "text",
          "label": "Citation"
        },
        {
          "id": "star_rating",
          "type": "range",
          "label": "Star Rating",
          "min": 0,
          "max": 5,
          "step": 1,
          "default": 5
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonials",
      "category": "Content"
    }
  ]
}
{% endschema %}
